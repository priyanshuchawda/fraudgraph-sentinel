[project]
name = "fraudgraph-sentinel"
version = "0.1.0"
description = "Graph-powered cyber-fraud investigation agent on Neo4j Aura"
requires-python = ">=3.11"
dependencies = [
  "neo4j>=5.28,<6",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
